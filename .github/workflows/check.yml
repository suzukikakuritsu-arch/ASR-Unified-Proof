name: ASR Proof CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      # これだけで Lean 4 環境は整います。
      - uses: leanprover/lean-action@v1

      # 前のステップで elan がパスを通しているので、そのまま叩けるはず。
      - name: Build
        run: lake build
